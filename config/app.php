<?php

/**
 * Основний конфігураційний файл Laravel додатку
 * Містить базові налаштування додатку, які визначають його поведінку
 */

return [

    /*
    |--------------------------------------------------------------------------
    | Назва додатку
    |--------------------------------------------------------------------------
    |
    | Це значення є назвою вашого додатку, яка буде використовуватися коли
    | фреймворк потребує розмістити назву додатку в повідомленні або
    | інших UI елементах, де потрібно відобразити назву додатку.
    |
    */

    'name' => env('APP_NAME', 'Laravel'),

    /*
    |--------------------------------------------------------------------------
    | Середовище додатку
    |--------------------------------------------------------------------------
    |
    | Це значення визначає "середовище" в якому зараз працює ваш додаток.
    | Це може визначати як ви надаєте перевагу налаштувати різні
    | сервіси, які використовує додаток. Встановіть це у вашому ".env" файлі.
    |
    */

    'env' => env('APP_ENV', 'production'),

    /*
    |--------------------------------------------------------------------------
    | Режим налагодження додатку
    |--------------------------------------------------------------------------
    |
    | Коли ваш додаток знаходиться в режимі налагодження, детальні повідомлення
    | про помилки зі стеком викликів будуть показані на кожній помилці, що
    | виникає в додатку. Якщо вимкнено, показується проста загальна сторінка помилки.
    |
    */

    'debug' => (bool) env('APP_DEBUG', false),

    /*
    |--------------------------------------------------------------------------
    | URL додатку
    |--------------------------------------------------------------------------
    |
    | Цей URL використовується консоллю для правильного генерації URL при
    | використанні інструменту командного рядка Artisan. Ви повинні встановити
    | це в корінь додатку, щоб воно було доступне в командах Artisan.
    |
    */

    'url' => env('APP_URL', 'http://localhost'),

    /*
    |--------------------------------------------------------------------------
    | Часовий пояс додатку
    |--------------------------------------------------------------------------
    |
    | Тут ви можете вказати часовий пояс за замовчуванням для вашого додатку,
    | який буде використовуватися функціями PHP date та date-time. Часовий пояс
    | встановлений на "UTC" за замовчуванням, оскільки він підходить для більшості випадків.
    |
    */

    'timezone' => 'UTC',

    /*
    |--------------------------------------------------------------------------
    | Конфігурація локалі додатку
    |--------------------------------------------------------------------------
    |
    | Локаль додатку визначає локаль за замовчуванням, яка буде використовуватися
    | методами перекладу / локалізації Laravel. Ця опція може бути встановлена
    | на будь-яку локаль, для якої ви плануєте мати рядки перекладу.
    |
    */

    'locale' => env('APP_LOCALE', 'en'),

    // Резервна локаль (використовується якщо основна локаль недоступна)
    'fallback_locale' => env('APP_FALLBACK_LOCALE', 'en'),

    // Локаль для генерації фейкових даних
    'faker_locale' => env('APP_FAKER_LOCALE', 'en_US'),

    /*
    |--------------------------------------------------------------------------
    | Ключ шифрування
    |--------------------------------------------------------------------------
    |
    | Цей ключ використовується сервісами шифрування Laravel і повинен бути
    | встановлений на випадковий 32-символьний рядок, щоб забезпечити
    | безпеку всіх зашифрованих значень. Ви повинні зробити це перед
    | розгортанням додатку.
    |
    */

    'cipher' => 'AES-256-CBC',

    'key' => env('APP_KEY'),

    // Попередні ключі шифрування (для ротації ключів)
    'previous_keys' => [
        ...array_filter(
            explode(',', env('APP_PREVIOUS_KEYS', ''))
        ),
    ],

    /*
    |--------------------------------------------------------------------------
    | Драйвер режиму обслуговування
    |--------------------------------------------------------------------------
    |
    | Ці опції конфігурації визначають драйвер, який використовується для
    | визначення та управління статусом "режиму обслуговування" Laravel.
    | Драйвер "cache" дозволить контролювати режим обслуговування
    | на кількох машинах.
    |
    | Підтримувані драйвери: "file", "cache"
    |
    */

    'maintenance' => [
        'driver' => env('APP_MAINTENANCE_DRIVER', 'file'),
        'store' => env('APP_MAINTENANCE_STORE', 'database'),
    ],

];
